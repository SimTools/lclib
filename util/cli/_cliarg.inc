C   31/03/87 703312235  MEMBER NAME  @CLIARG  (FORT)     M  FORTRAN
C
      INTEGER * 4  MAXARG, MAXBUF
      PARAMETER( MAXARG = 10, MAXBUF = 512 )
C
      COMMON / CLIARG / NARGS,IARGS,LARGS,IARGP,LARGBU
C
      INTEGER * 4  NARGS, IARGP
      INTEGER * 4  IARGS( MAXARG ), LARGS( MAXARG )
      LOGICAL * 1  LARGBU( MAXBUF )
C
      CHARACTER * ( MAXBUF )  ARGBUF
      EQUIVALENCE( LARGBU,ARGBUF )
C
